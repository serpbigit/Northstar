{
  "name": "northstar-ts",
  "version": "1.0.0",
  "description": "Polaris Agent (Google Apps Script / TypeScript PoC)",
  "main": "index.js",
  "scripts": {
    "prebuild": "rm -rf dist",
    "build": "tsc && cp appsscript.json dist/ && chmod +x build-unified.sh && ./build-unified.sh",
    "push": "echo 'Deployment is now MANUAL. See northstarUnified.gs for content.'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@google/clasp": "^2.4.1"
  },
  "devDependencies": {
    "@types/google-apps-script": "^1.0.84",
    "typescript": "^5.4.5"
  }
}
